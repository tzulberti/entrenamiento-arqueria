// Tiene todas las instancias de las cosas que estan en consts
// para no crear mucha de las mismas.
var consts = {
    tipoTorneos: new TipoTorneos(),

    // indica el path basico para todos los llamados ajax correspondientes.
    BASE_API_URL: '/api/v01/'
};
